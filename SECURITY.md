# セキュリティポリシー

## 本プロジェクトの設計上の前提

**Muscle Chair は、閉じた LAN 環境でのイベント運用を前提としたハッカソン作品です。インターネットに公開して運用することは想定していません。**

以下は既知の設計上の制約であり、脆弱性報告の対象外です。

| 項目 | 内容 |
|------|------|
| 認証・認可 | 実装していません。API エンドポイントは誰でも呼び出せます |
| CORS | `backend/main.py` で全オリジンを許可しています（`allow_origins=["*"]`） |
| 通信の暗号化 | HTTP / WebSocket を平文で使用しています |
| 特権実行 | `backend/output_server.py` は受信をトリガーに `sudo node` でスクリプトを実行します |

とくに `output_server.py` は、`/trigger_action` へのリクエストを受け取ると root 権限で Node.js スクリプトを起動します。実行するスクリプト名はコード内の定数で固定されており、リクエスト内容から組み立てられることはありませんが、**このサーバーを到達可能な範囲に置くことは、そのまま演出を誰でも起動できることを意味します。**

### 運用上のお願い

- 外部から到達できないネットワーク（イベント会場のローカル LAN 等）でのみ動作させてください
- ルーターのポートフォワーディングやトンネリングサービス経由で公開しないでください
- インターネットに接続された環境で使う必要がある場合は、リバースプロキシでの認証付与、ファイアウォールによる送信元制限などを各自で追加してください

## 脆弱性の報告

上記の前提を踏まえてなお報告すべき問題（例: 想定外のコマンド実行経路、依存ライブラリの既知の脆弱性）を発見した場合は、GitHub の [Security Advisories](https://github.com/jade-summer/muscle_chair/security/advisories/new) から非公開で報告してください。

公開 Issue での報告は避けてください。

学生の個人プロジェクトのため、返信までに時間がかかる場合があります。ご了承ください。

## サポート対象

本リポジトリはハッカソン作品のアーカイブとして公開しているもので、継続的なセキュリティ更新は行っていません。最新のコミットのみがサポート対象です。
